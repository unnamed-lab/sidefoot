node_modules/
dist/
*.log

# Secrets
.env
.env.local

# Cached devnet session (jwt + apiToken + subscribe txSig)
.session.json

# Recorded replay datasets + observability logs — regenerate by running
data/*.jsonl
data/**/*.jsonl
!data/.gitkeep
